import 'dart:ui';

class ThemeRepository {

  //Here we can save theme locally to sharedPreference or we can save it online with an http request
  //
  void saveTheme({required Brightness brightness, required Color primaryColor, required String textTheme}) {

    // save theme locally

  }

}